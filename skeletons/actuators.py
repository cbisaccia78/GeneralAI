class Actuator:
    """
    Following our brain-arm-bottle example defined in actions.py,
    the actuator filters directives(voltage) at a particular time t. voltage(t) may indicate that the arm needs to
     contract muscles m1, m2, ..., mk in order to move the center of mass
    of the arm by delta. the actuator must implement the map (voltage(t)) of the directives to these consequences
    """
    def __init__(self):
        return

    def act(self, action):
        # do
        return
